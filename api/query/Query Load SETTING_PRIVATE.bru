meta {
  name: Query Load SETTING_PRIVATE
  type: http
  seq: 11
}

get {
  url: {{baseUrl}}/query-load/SETTING_PRIVATE
}

params:query {
  type: integration
  active: true
}

headers {
  Authorization: Bearer {{token}}
  x-tenant-id: {{xTenantId}}
}
